UPDATE {{table}} SET {{#columns}}{{.}}=${{.}}, {{/columns}} updated_at=DATETIME('now') WHERE id=$id RETURNING{{#returning}} {{.}}{{#unless @last}},{{/unless}}{{/returning}}{{^returning}} *{{/returning}};