<%- include('./_head.ejs') %>
  <div class="container mt-4">
    <h1>Theaters</h1>
    <table class="table mb-4">
      <thead>
        <th>Created At</th>
        <th>Updated At</th>
        <th>Name</th>
        <th>Location</th>
        <th colspan="3">Actions</th>
      </thead>
      <tbody>
        <% for (const it of theaters) { %>
        <tr>
          <td><%= formatDate(it.created_at) %></td>
          <td><%= formatDate(it.updated_at) %></td>
          <td><%= it.name %></td>
          <td><%= it.location %></td>
          <td><a href="/theaters/<%= it.id %>" class="btn btn-primary">Show</a></td>
          <td><a href="/theaters/edit/<%= it.id %>" class="btn btn-success">Edit</a></td>
          <td>
            <form action="/theaters/delete/<%= it.id %>" method="POST">
              <%- include(locals.LayoutPages._csrf) %>
              <button type="submit" class="btn btn-danger">
                Delete
              </button>
            </form>
          </td>
        </tr>
        <% } %>
      </tbody>
    </table>
    <a href="/theaters/new" class="btn btn-primary">New Theater</a>
  </div>
  <%- include(locals.LayoutPages._tail) %>